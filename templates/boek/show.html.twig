{% extends 'base.html.twig' %}

{% block title %}Boek{% endblock %}

{% block body %}
    <h1>Boek</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boek.id }}</td>
            </tr>
            <tr>
                <th>ISBN</th>
                <td>{{ boek.ISBN }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ boek.Auteur }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ boek.Title }}</td>
            </tr>
            <tr>
                <th>Prijs</th>
                <td>{{ boek.Prijs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('boek_index') }}">back to list</a>

    <a href="{{ path('boek_edit', {'id': boek.id}) }}">edit</a>

    {{ include('boek/_delete_form.html.twig') }}
{% endblock %}
