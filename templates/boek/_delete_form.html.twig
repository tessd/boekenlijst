<form method="post" action="{{ path('boek_delete', {'id': boek.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boek.id) }}">
    <button class="btn">Delete</button>
</form>
